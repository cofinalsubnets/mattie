(library (test runner)
         (export run-all-tests)
         (import (rnrs) (test util)
                 (test mattie parsers stateful)
                 (test mattie program-builder)
                 (test mattie parsers language))
  (define (run-all-tests)
    (run-test-suite "stateful parser tests" stateful-parser-tests)
    (run-test-suite "language parser tests" language-parser-tests)
    (run-test-suite "program builder tests" program-builder-tests)
    ))
